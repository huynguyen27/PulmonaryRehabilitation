package com.example.pulmonaryrehabilitation.activity.media

import com.example.pulmonaryrehabilitation.R

enum class VideoNames(val resource: Int) {
    // why is the paramater Int? I guess that's how Android identifies resource items
    VIDEO2(R.raw.testvideo2),
    VIDEO1(R.raw.testvideo2),
    TEST(R.raw.testvideo2)
}
